## Roboter

A simple,  clean, extensible differential robot simulator where robot moves, publish datas, receives commands and everything is observable, loggable and replayable.

### Building
- Linux
    ``` 
        cmake -B build -G Ninja
        ninja -C build
        ./bin/Linux64/Release/Roboter
    ```

- Windows
    ```

    ```